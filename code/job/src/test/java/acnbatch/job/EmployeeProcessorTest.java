package acnbatch.job;

import acnbatch.job.domain.EmployeeInputRecord;
import org.junit.Test;

import static org.junit.Assert.*;

public class EmployeeProcessorTest {

    /*
    @Test
    public void testProcessItem() throws Exception {
        EmployeeProcessor processor = new EmployeeProcessor();
        Object result;

        EmployeeInputRecord record = new EmployeeInputRecord();
        record.setName("Blystad, Arne Christian");
        record.setEmail("try.catch@blystad.me");
        record.setPersonalNumber("100");
        record.setPhone("38493802");

        result = processor.processItem(record);

        assertEquals(record, result);

        record.setEmail("kakeblystad.me");

        result = processor.processItem(record);

        assertNull(result);
    }
    */
}
